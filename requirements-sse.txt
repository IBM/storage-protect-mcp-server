# requirements-sse.txt — SSE / HTTP transport extras  (project.optional-dependencies.sse)
# Usage: pip install -r requirements.txt -r requirements-sse.txt
-r requirements.txt

mcp[sse]
starlette>=0.27.0
uvicorn>=0.23.0
httpx>=0.24.0
PyJWT>=2.8.0
