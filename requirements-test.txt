# requirements-test.txt — test extras  (project.optional-dependencies.test)
# Usage: pip install -r requirements.txt -r requirements-test.txt
-r requirements.txt

pytest>=7.0.0
pytest-asyncio>=0.21.0
